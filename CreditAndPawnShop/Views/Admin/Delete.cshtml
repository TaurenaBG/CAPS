@model CAPS.ViewModels.PawnShopsViewModel

<h2>Delete PawnShop</h2>
<form method="post">
    <div>Are you sure you want to delete this PawnShop?</div>
    <div>
        <strong>@Model.Name</strong> - @Model.City
    </div>
    <button type="submit">Yes, Delete</button>
</form>
<a href="@Url.Action("Index", "PawnShops")">Cancel</a>

